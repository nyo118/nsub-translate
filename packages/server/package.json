{
  "name": "@lst/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local backend for Live Subtitle Translator (Phase 0: mock subtitle session over WebSocket).",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/websocket": "^11.3.1",
    "@lst/protocol": "0.1.0",
    "fastify": "^5.12.5",
    "node-llama-cpp": "^3.21.1",
    "sherpa-onnx-node": "^1.13.8"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "tsx": "^4.20.0",
    "ws": "^8.21.3"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
